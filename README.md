# Discord Bot Project

A Discord bot built with Discord.js for handling API interactions and AI integrations.

## Setup
1. Clone the repository
2. Install dependencies: `npm install`
3. Rename `.env.example` to `.env` and configure your token

## Usage
- Start the bot with `npm run start`
- Commands and features are documented in the codebase